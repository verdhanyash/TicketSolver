# TicketSolver frontend environment variables (Vite exposes VITE_* to the client).
# Copy to `.env`. The dev server proxies /api and /ws to the backend by default,
# so these are only needed when pointing at a non-default backend.
VITE_API_BASE=/api
VITE_WS_URL=/ws/dashboard
