import { DashboardPage } from "./pages/DashboardPage";

// Session 1 scaffold: single page. Routing (dashboard <-> ticket detail) is wired
// in a later session once data flows exist.
export default function App() {
  return <DashboardPage />;
}
