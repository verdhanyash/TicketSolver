// Cost panel (FR-16.2) — placeholder until cost aggregation lands.
// Will compare ML-routed spend vs. an "always strongest model" baseline (FR-14 story).
export function CostPanel() {
  return (
    <section className="panel">
      <h2>Cost per Ticket</h2>
      <p>No data yet — this will show spending and how much the smart model
        routing is saving.</p>
    </section>
  );
}
